all:
	@echo "making third party libs"
	cd TooN && make
	cd libcvd && make
	cd gvars && make
	cd agast && make


clean:
	@echo "cleaning up third party libs"
	cd libcvd && make clean  
	cd gvars && make clean 
	cd agast && make clean